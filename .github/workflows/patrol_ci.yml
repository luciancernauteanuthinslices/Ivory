# name: Flutter CI/CD

# on:
#   push:
#     branches: [main, develop]
#   pull_request:
#     branches: [main, develop]
#   workflow_dispatch:

# env:
#   FLUTTER_VERSION: "3.35.7"
#   JAVA_VERSION: "17"

# jobs:
#   # Static analysis and unit tests - fast feedback
#   analyze-and-test:
#     name: Analyze & Unit Test
#     runs-on: ubuntu-latest
#     timeout-minutes: 15

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Setup Java
#         uses: actions/setup-java@v4
#         with:
#           distribution: "temurin"
#           java-version: ${{ env.JAVA_VERSION }}

#       - name: Setup Flutter
#         uses: subosito/flutter-action@v2
#         with:
#           flutter-version: ${{ env.FLUTTER_VERSION }}
#           channel: "stable"
#           cache: true

#       - name: Get dependencies
#         run: flutter pub get

#       - name: Verify formatting
#         run: dart format --output=none --set-exit-if-changed .

#       - name: Analyze code
#         run: flutter analyze --no-fatal-infos

#       # Unit tests disabled - no unit tests yet, only integration tests
#       # - name: Run unit tests
#       #   run: flutter test --coverage
#       #
#       # - name: Upload coverage to Codecov
#       #   uses: codecov/codecov-action@v4
#       #   with:
#       #     files: ./coverage/lcov.info
#       #     fail_ci_if_error: false

#   # Build Android APK (debug)
#   build-android:
#     name: Build Android APK
#     runs-on: ubuntu-latest
#     needs: analyze-and-test
#     timeout-minutes: 30

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Setup Java
#         uses: actions/setup-java@v4
#         with:
#           distribution: "temurin"
#           java-version: ${{ env.JAVA_VERSION }}

#       - name: Setup Flutter
#         uses: subosito/flutter-action@v2
#         with:
#           flutter-version: ${{ env.FLUTTER_VERSION }}
#           channel: "stable"
#           cache: true

#       - name: Get dependencies
#         run: flutter pub get

#       - name: Build APK
#         run: flutter build apk --debug

#       - name: Upload APK artifact
#         uses: actions/upload-artifact@v4
#         with:
#           name: android-debug-apk
#           path: build/app/outputs/flutter-apk/app-debug.apk
#           retention-days: 7

#   # Android Integration Tests with optimizations
#   integration-test-android:
#     name: Integration Tests (Android)
#     runs-on: ubuntu-latest
#     needs: analyze-and-test
#     timeout-minutes: 45
#     strategy:
#       fail-fast: false
#       matrix:
#         api-level: [29] # API 29 is most stable on GitHub Actions
#         arch: [x86_64]

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Setup Java
#         uses: actions/setup-java@v4
#         with:
#           distribution: "temurin"
#           java-version: ${{ env.JAVA_VERSION }}

#       - name: Setup Flutter
#         uses: subosito/flutter-action@v2
#         with:
#           flutter-version: ${{ env.FLUTTER_VERSION }}
#           channel: "stable"
#           cache: true

#       - name: Get dependencies
#         run: flutter pub get

#       - name: Setup Patrol CLI
#         run: |
#           dart pub global activate patrol_cli
#           echo "$HOME/.pub-cache/bin" >> $GITHUB_PATH

#       - name: Enable KVM group perms
#         run: |
#           echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm4all.rules
#           sudo udevadm control --reload-rules
#           sudo udevadm trigger --name-match=kvm

#       - name: Gradle cache
#         uses: gradle/actions/setup-gradle@v3
#         with:
#           cache-read-only: ${{ github.ref != 'refs/heads/main' }}

#       - name: AVD cache
#         uses: actions/cache@v4
#         id: avd-cache
#         with:
#           path: |
#             ~/.android/avd/*
#             ~/.android/adb*
#           key: avd-${{ matrix.api-level }}-${{ matrix.arch }}

#       - name: Create AVD and generate snapshot for caching
#         if: steps.avd-cache.outputs.cache-hit != 'true'
#         uses: reactivecircus/android-emulator-runner@v2
#         with:
#           api-level: ${{ matrix.api-level }}
#           arch: ${{ matrix.arch }}
#           target: google_apis
#           force-avd-creation: false
#           emulator-options: -no-window -gpu swiftshader_indirect -noaudio -no-boot-anim -camera-back none
#           disable-animations: true
#           script: echo "Generated AVD snapshot for caching."

#       - name: Run integration tests
#         uses: reactivecircus/android-emulator-runner@v2
#         with:
#           api-level: ${{ matrix.api-level }}
#           arch: ${{ matrix.arch }}
#           target: google_apis
#           force-avd-creation: false
#           emulator-options: -no-snapshot-save -no-window -gpu swiftshader_indirect -noaudio -no-boot-anim -camera-back none
#           disable-animations: true
#           script: |
#             adb logcat > logcat.txt &
#             patrol test --verbose --target integration_test/app_navigation_test.dart || true
#             patrol test --verbose --target integration_test/simple_test.dart

#       - name: Upload test results
#         if: always()
#         uses: actions/upload-artifact@v4
#         with:
#           name: integration-test-results-android-${{ matrix.api-level }}
#           path: |
#             build/app/outputs/androidTest-results/
#             logcat.txt
#           retention-days: 14

#       - name: Upload screenshots on failure
#         if: failure()
#         uses: actions/upload-artifact@v4
#         with:
#           name: integration-test-screenshots-android-${{ matrix.api-level }}
#           path: |
#             integration_test/screenshots/
#             screenshots/
#           retention-days: 14

#   # iOS Build (macOS only)
#   build-ios:
#     name: Build iOS
#     runs-on: macos-14
#     needs: analyze-and-test
#     timeout-minutes: 30

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Setup Flutter
#         uses: subosito/flutter-action@v2
#         with:
#           flutter-version: ${{ env.FLUTTER_VERSION }}
#           channel: "stable"
#           cache: true

#       - name: Get dependencies
#         run: flutter pub get

#       - name: Build iOS (no codesign)
#         run: flutter build ios --no-codesign --debug

#       - name: Upload iOS build artifact
#         uses: actions/upload-artifact@v4
#         with:
#           name: ios-debug-build
#           path: build/ios/
#           retention-days: 7

#   # iOS Integration Tests - INFORMATIONAL ONLY
#   # These tests may fail due to Patrol/simulator issues on CI
#   # Failures here are non-blocking and should be investigated locally
#   integration-test-ios:
#     name: Integration Tests (iOS - Informational)
#     runs-on: macos-14
#     needs: analyze-and-test
#     timeout-minutes: 45
#     continue-on-error: true
#     strategy:
#       fail-fast: false
#       matrix:
#         device: ["iPhone 15 Pro"]
#         os_version: ["17.5"]

#     steps:
#       - name: ⚠️ iOS Test Limitations
#         run: |
#           echo "::warning::iOS tests on CI are informational only"
#           echo "::warning::These tests may fail due to Patrol/CI limitations"
#           echo "::warning::Test failures here do not block merging"
#           echo "Please run tests locally to verify iOS functionality"

#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Setup Flutter
#         uses: subosito/flutter-action@v2
#         with:
#           flutter-version: ${{ env.FLUTTER_VERSION }}
#           channel: "stable"
#           cache: true

#       - name: Get dependencies
#         run: flutter pub get

#       - name: Setup Patrol CLI
#         run: |
#           dart pub global activate patrol_cli
#           echo "$HOME/.pub-cache/bin" >> $GITHUB_PATH

#       - name: Start iOS Simulator
#         uses: futureware-tech/simulator-action@v3
#         with:
#           model: ${{ matrix.device }}
#           os_version: ${{ matrix.os_version }}
#           erase_before_boot: true
#           shutdown_after_job: true

#       - name: Run integration tests
#         continue-on-error: true
#         run: |
#           echo "Running integration tests on iOS simulator"
#           set +e

#           # Wait for simulator to be fully ready
#           sleep 5

#           echo "Running simple_test.dart (smoke test)..."
#           patrol test --target integration_test/simple_test.dart --verbose
#           SIMPLE_EXIT=$?

#           if [ $SIMPLE_EXIT -eq 0 ]; then
#             echo "::notice::Simple test passed! ✅"
#           else
#             echo "::error::Simple test failed with exit code $SIMPLE_EXIT"
#             echo "::warning::iOS integration tests may have stability issues on CI"
#           fi

#           echo ""
#           echo "Running app_navigation_test.dart..."
#           patrol test --target integration_test/all_ --verbose
#           NAV_EXIT=$?

#           if [ $NAV_EXIT -eq 0 ]; then
#             echo "::notice::Navigation test passed! ✅"
#           else
#             echo "::warning::Navigation test failed (exit code $NAV_EXIT)"
#           fi

#           # iOS tests are informational only - always succeed
#           echo "::notice::iOS tests completed (exit codes: simple=$SIMPLE_EXIT, nav=$NAV_EXIT)"
#           echo "::notice::iOS integration tests on CI are informational and may be flaky"
#           echo "::notice::Please verify iOS functionality locally if tests fail here"
#           exit 0

#       - name: Upload test results
#         if: always()
#         uses: actions/upload-artifact@v4
#         with:
#           name: integration-test-results-ios
#           path: |
#             build/ios/
#           retention-days: 14

#       - name: Upload screenshots on failure
#         if: failure()
#         uses: actions/upload-artifact@v4
#         with:
#           name: integration-test-screenshots-ios
#           path: |
#             integration_test/screenshots/
#             screenshots/
#           retention-days: 14

#   # Summary job
#   ci-success:
#     name: CI Success
#     runs-on: ubuntu-latest
#     needs: [
#         analyze-and-test,
#         build-android,
#         integration-test-android,
#         build-ios,
#         # integration-test-ios is optional (continue-on-error: true)
#       ]
#     if: always()

#     steps:
#       - name: Check job status
#         run: |
#           if [[ "${{ needs.analyze-and-test.result }}" != "success" ]]; then
#             echo "Analyze and test job failed"
#             exit 1
#           fi
#           if [[ "${{ needs.build-android.result }}" != "success" ]]; then
#             echo "Android build job failed"
#             exit 1
#           fi
#           if [[ "${{ needs.integration-test-android.result }}" != "success" ]]; then
#             echo "Android integration tests failed"
#             exit 1
#           fi
#           if [[ "${{ needs.build-ios.result }}" != "success" ]]; then
#             echo "iOS build job failed"
#             exit 1
#           fi

#           # iOS integration tests are informational only
#           if [[ "${{ needs.integration-test-ios.result }}" != "success" ]]; then
#             echo "::warning::iOS integration tests failed (informational only, non-blocking)"
#           fi

#           echo "All critical jobs passed successfully!"