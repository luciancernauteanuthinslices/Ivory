name: Smoke (runs on push to this branch)

on:
  push:
    branches: ["feature/patrol-ci"]   # change if your branch name differs
  workflow_dispatch:

jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Workflow is alive on $GITHUB_REF"
